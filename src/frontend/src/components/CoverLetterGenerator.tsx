// CoverLetterGenerator — the cover letter is handled within AgentWorkflow
// The strict cover letter prompt rules are enforced in AgentWorkflow's cover-letter-builder step
export { AgentWorkflow as CoverLetterGenerator } from "./workflow/AgentWorkflow";
